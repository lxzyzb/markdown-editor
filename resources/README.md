# 静态资源目录

放置应用图标、打包资源等：

- `icon.ico` (Windows)
- `icon.icns` (macOS)
- `icon.png` (Linux / 通用)
- `installerIcon.ico` (安装包图标)
- `uninstallerIcon.ico` (卸载图标)

> 实际图标文件需自行准备后放入本目录，并在 `electron-builder.yml` 中正确配置。
